import "./Form.less"

import FormAuth from "../FormAuth/FormAuth";

const Form = () => {
    return (
        <main className="main">
            <FormAuth className="form-auth"></FormAuth>
        </main>
    );
};

export default Form;